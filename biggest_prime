#include<stdio.h>
#include<math.h>
int main(){ 
int n,c,a;
printf("Enter the number: ");
scanf("%d",&n);
for(int i=1;i<=n;+i++){
    c=0;
    for(int j=1;j<=i;j++){
        if(i%j==0)
        c++;}
    if(c==2)
    a=i;}
printf("%d ",a);
return 0;}
